# ⚡ GitHub快速操作指南

## 🎯 您现在在GitHub Dashboard

按照以下3个简单步骤：

## 步骤1：创建新仓库（1分钟）

1. **点击页面右上角的 "+" 图标**
2. **选择 "New repository"**
3. **填写信息**：
   - Repository name: `starbucks-admin`
   - 选择：**Public**
   - ❌ 不要勾选任何选项（README、.gitignore等）
4. **点击绿色的 "Create repository" 按钮**

## 步骤2：复制仓库URL（10秒）

创建完成后，页面会显示一个URL，类似：
```
https://github.com/您的用户名/starbucks-admin.git
```

**请复制这个URL！**

## 步骤3：在电脑上上传代码（2分钟）

在您的Windows电脑上，打开PowerShell，运行：

```powershell
# 进入项目目录
cd D:\re_order

# 初始化Git（如果还没有）
git init

# 添加所有文件
git add .

# 提交代码
git commit -m "Initial commit"

# 设置主分支
git branch -M main

# 添加远程仓库（替换成您刚才复制的URL）
git remote add origin https://github.com/您的用户名/starbucks-admin.git

# 上传代码
git push -u origin main
```

## ⚠️ 如果提示需要登录

1. 用户名：输入您的GitHub用户名
2. 密码：**不是您的GitHub密码！**
   - 需要使用Personal Access Token
   - 创建方法：GitHub → Settings → Developer settings → Personal access tokens → Generate new token
   - 勾选 `repo` 权限
   - 复制生成的token作为密码

## ✅ 完成标志

上传成功后：
- GitHub页面会显示您的所有文件
- 可以看到 `client`、`server` 等文件夹

## 🚀 下一步

代码上传后，下一步是部署到Vercel：
1. 访问：https://vercel.com/
2. 使用GitHub登录
3. 导入刚创建的仓库
4. 部署！

---

**现在开始第一步：点击右上角的 "+" 创建新仓库！**
